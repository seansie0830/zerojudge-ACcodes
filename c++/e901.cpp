#include "bits/stdc++.h"
using namespace std;
int main (){
    int n,i,temp,del=0,j;
    cin>>n;
    vector<int> m,f;
    for(int i = 0; i < n;i++){
        cin>>temp;
        m.push_back(temp);
    }
    for(int i = 0; i<n;i++){
        cin>>temp;
        f.push_back(temp);
    }
    while(del<5){
        for(i=0;i<m.size();i++){
            
        }
    }
}